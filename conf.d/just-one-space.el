(when (or
       ;; peccu-p
       win-env-p
       ;; wsl-p
       )
  ;; default
  ;; (global-set-key (kbd "M-SPC") 'just-one-space)
  ;; for spotlight/alfred/zazu
  (global-set-key (kbd "s-SPC") 'just-one-space)
  )
