(when (or
       peccu-p
       win-env-p
       wsl-p
       )
  ;; packages version
  ;; (require-with-install 'xxx-mode)
  ;; submodule version
  ;; (add-submodule-to-load-path "git/xxx-mode")
  ;; (require 'xxx-mode)

  )
